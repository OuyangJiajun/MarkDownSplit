# MarkdownSplit 架构讨论记录

## 1. 为什么这样分层设计

这个项目的主流程被拆成四个阶段：

1. 解析 Markdown 结构
2. 规划输出文件布局
3. 重写标题和图片
4. 通过 CLI 触发整个流程

对应到代码里分别是：

- `parser.py` 负责解析
- `splitter.py` 负责布局规划
- `rewriter.py` 负责内容修正
- `cli.py` 负责命令行入口

这种设计的核心优点是：**每一步都可以单独理解、单独测试、单独替换。**

---

## 2. 为什么先解析成树，而不是边读边写

`parser.py` 先把文本转成标题树。

这样做的原因是：如果不先变成树，而是直接按文本流拆分，会很难处理：

- 标题嵌套关系
- 某个章节是否应该继续拆分
- 需要把整篇文档当成一个结构来判断
- 代码块里的 `#` 不能误判为标题

树结构让这些问题都变得自然很多：

- 父子关系明确
- 可以递归处理
- 可以方便地判断一个块的大小
- 可以决定是“写成一个文件”还是“继续拆”

---

## 3. 为什么输出规划和真正写文件要分开

`splitter.py` 只负责“决定输出什么”，不负责“怎么写进去”。

它的输出是 `OutputFile` 计划对象，里面只包含：

- 输出目录片段
- 文件名
- 文件内容

这样拆分的好处是：

- 可测试
- 可替换
- 关注点单一

以后如果想改目录规则、改文件命名、改拆分阈值策略，只需要动 `splitter.py`。

---

## 4. 为什么再单独有 `rewriter.py`

`rewriter.py` 只做输出前最后的整理：

- 提升标题层级
- 复制本地图片
- 重写图片路径

这两个问题都属于“写文件前的修正”，单独放在 `rewriter.py` 非常合理。

---

## 5. 为什么 `cli.py` 只是薄入口

CLI 层只负责：

- 解析参数
- 校验输入
- 串起主流程
- 输出结果

它不应该承载复杂业务逻辑。这样做的好处是：

- 更容易维护
- 更容易测试
- 更容易以后加 Web UI、API 或脚本调用接口

---

## 6. 这个架构的整体优点

### 6.1 模块边界清晰
每个模块只做一类事。

### 6.2 逻辑链条顺
输入文本 → 树结构 → 文件规划 → 文件写出

### 6.3 便于测试
解析、拆分、重写都能单独测试。

### 6.4 容易继续演进
如果以后想支持更多 Markdown 语法、自定义命名规则、自定义资源目录或更复杂的拆分策略，只需要替换对应层，不用推翻整个项目。

---

## 7. 还能怎么优化

### 7.1 统一数据模型
可以把核心结构更明确地做成 `@dataclass`，例如：

- `Block`
- `OutputFile`

这样会更简洁，也更利于调试和测试。

### 7.2 给拆分规则做成可配置策略
当前拆分逻辑主要是：

- 有子章节
- 超过 `threshold`

可以进一步支持：

- 按标题层级拆
- 按总行数拆
- 按子章节数量拆
- 不同层级使用不同阈值

### 7.3 增强文件名冲突处理
可以进一步做到：

- 按层级路径生成唯一名字
- 目录和文件统一冲突检测
- 图片去重时保留更完整的原始路径信息

### 7.4 更完整的 Markdown 解析
当前解析方式主要处理：

- ATX 标题
- fenced code block
- 图片

如果项目目标更大，可以考虑支持：

- Setext 标题
- blockquote
- HTML 结构
- 脚注
- 目录标记
- 更严格的 Markdown AST

### 7.5 把图片复制改成更稳健的资源映射
可以考虑：

- 保留原相对目录结构
- 用哈希做资源名
- 支持同名不同图的更稳定映射
- 生成资源索引表

### 7.6 补一个更标准的打包配置
例如加入：

- `pyproject.toml`
- 明确入口脚本
- 规范测试配置
- 规范版本和依赖

### 7.7 引入更系统的测试分层
建议覆盖：

- 单元测试：`utils`、`parser`
- 行为测试：`splitter`
- 集成测试：整个 CLI 流程
- 边界测试：空文档、重复标题、超深层级、图片冲突

---

## 8. 结论

这个项目的架构设计是合理的，因为它遵循了一个很好的原则：

> 先把文档理解成结构，再根据结构决定输出布局，最后做输出修正。

它不是把所有逻辑堆在一个函数里，而是分成了“解析、规划、重写、入口”四层，这就是它为什么比较容易维护，也容易继续扩展。

---

## 9. 后续优化优先顺序

建议按这个顺序继续演进：

1. 继续补测试
2. 统一数据结构风格，尽量用 dataclass
3. 增强文件名/资源冲突处理
4. 把拆分规则做成可配置
5. 再考虑更完整的 Markdown 解析
